#summary {
    height: 1;
    padding: 0 1;
    color: $text-muted;
}

#active-table {
    height: auto;
    min-height: 5;
    max-height: 50%;
}

#history-table {
    height: 1fr;
}

#active-label, #history-label {
    height: 1;
    color: $text-muted;
}
